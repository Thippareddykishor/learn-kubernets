apiVersion: v1
kind: Service
metadata:
  name: {{ include "learn-kubernets.fullname" . }}
  labels:
    {{- include "learn-kubernets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learn-kubernets.selectorLabels" . | nindent 4 }}
