apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learn-kubernets.fullname" . }}-test-connection"
  labels:
    {{- include "learn-kubernets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learn-kubernets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
